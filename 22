-- ========================================================
-- DỰ ÁN FLASHMART: KHỦNG HOẢNG CHIẾN DỊCH MARKETING
-- Dọn dẹp & Khôi phục báo cáo bị "đánh cắp" dữ liệu
-- ========================================================

-- 1. Tạo cơ sở dữ liệu và thiết lập môi trường
CREATE DATABASE IF NOT EXISTS flashmart_db;
USE flashmart_db;

-- 2. Tạo bảng dữ liệu
DROP TABLE IF EXISTS Orders;
DROP TABLE IF EXISTS Products;
DROP TABLE IF EXISTS Customers;

CREATE TABLE Customers (
    customer_id INT PRIMARY KEY,
    name VARCHAR(50)
);

CREATE TABLE Products (
    product_id INT PRIMARY KEY,
    product_name VARCHAR(50)
);

CREATE TABLE Orders (
    order_id INT PRIMARY KEY,
    customer_id INT,
    product_id INT
);

-- 3. Chèn dữ liệu thử nghiệm
INSERT INTO Customers VALUES 
(1, 'Alice'), 
(2, 'Bob'), 
(3, 'Charlie'); -- Charlie chưa từng mua hàng

INSERT INTO Products VALUES 
(101, 'Laptop'), 
(102, 'Mouse'), 
(103, 'Keyboard'); -- Keyboard chưa từng được mua

INSERT INTO Orders VALUES 
(1001, 1, 101), 
(1002, 1, 102), 
(1003, 2, 101);

-- ========================================================
-- BÁO CÁO 1: Lấy tất cả Khách hàng & Số lượng đơn hàng (Marketing)
-- Giải pháp: Dùng LEFT JOIN và COUNT(o.order_id)
-- Kết quả kỳ vọng: Alice (2), Bob (1), Charlie (0)
-- ========================================================
SELECT 
    c.customer_id, 
    c.name, 
    COUNT(o.order_id) AS total_orders
FROM Customers c
LEFT JOIN Orders o ON c.customer_id = o.customer_id
GROUP BY c.customer_id, c.name;

-- ========================================================
-- BÁO CÁO 2: Lấy các Sản phẩm chưa từng bán ra (Kho vận - Anti-Join)
-- Giải pháp: Dùng LEFT JOIN kết hợp WHERE o.order_id IS NULL
-- Kết quả kỳ vọng: 103 - Keyboard
-- ========================================================
SELECT 
    p.product_id, 
    p.product_name
FROM Products p
LEFT JOIN Orders o ON p.product_id = o.product_id
WHERE o.order_id IS NULL;
